package com.anavis.springboot;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

//import static org.assertj.core.api.Assertions.assertThat;
/*
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.request.MockMvcRequestBuilders;

import com.anavis.springboot.controller.PrenotazioneController;
*/
//import com.anavis.springboot.controller.PrenotazioneController;


@SpringBootTest
//@AutoConfigureMockMvc
class AnAvisApplicationTests {
	
//	@Autowired
//	private MockMvc mvc;

	@Test
	void contextLoads() throws Exception{
		//assertThat(prenotazioneController).isNotNull();
		//mvc.perform(MockMvcRequestBuilders.get("/sedi").accept(org.springframework.http.MediaType.ALL)).andExpect(status().isOk());
		//mvc.perform(MockMvcRequestBuilders.get("/sedi").accept(org.springframework.http.MediaType.APPLICATION_JSON)).andExpect(status().isOk());
	}

}
