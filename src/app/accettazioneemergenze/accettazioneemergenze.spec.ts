import { Accettazioneemergenze } from './accettazioneemergenze';

describe('Accettazioneemergenze', () => {
  it('should create an instance', () => {
    expect(new Accettazioneemergenze()).toBeTruthy();
  });
});
